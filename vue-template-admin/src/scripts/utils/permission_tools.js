// 权限 - 工具

/**
 * 获取用户权限菜单
 */
export function generateMenus(totalMenus, userMenus) {
    const result = []
    totalMenus.forEach((menus) => {
        console.log('menus :>> ', menus)
    })
    return result
}
